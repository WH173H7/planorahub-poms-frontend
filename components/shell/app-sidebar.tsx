import { NavSection } from '@/components/navigation/nav-section';
import type { NavigationSection } from '@/components/navigation/navigation.types';
import { Icon } from '@/components/ui/icon';
export function AppSidebar({navigation,path,collapsed,onToggle,onNavigate,mobile=false}:{navigation:NavigationSection[];path:string;collapsed:boolean;onToggle?:()=>void;onNavigate?:()=>void;mobile?:boolean}){return <aside className="sidebar" aria-label="Application navigation"><div className="sidebar-brand"><span className="brand-mark">P</span><span className="brand-copy">Planora<span>Hub</span></span>{mobile?<button className="mobile-close" aria-label="Close navigation" onClick={onNavigate}><Icon name="close"/></button>:null}</div><nav className="sidebar-nav">{navigation.map(section=><NavSection key={section.label} section={section} path={path} collapsed={collapsed} onNavigate={onNavigate}/>)}</nav>{!mobile?<div className="sidebar-footer"><button className="collapse-button" type="button" onClick={onToggle} aria-label={collapsed?'Expand sidebar':'Collapse sidebar'} aria-pressed={collapsed}><Icon name="chevron"/></button></div>:null}</aside>}

'use client';import type { ReactNode } from 'react';import { useState } from 'react';export function DropdownMenu({trigger,children,label}:{trigger:ReactNode;children:ReactNode;label:string}){const [open,setOpen]=useState(false);return <div className="user-menu-wrap"><button className="user-trigger" aria-haspopup="menu" aria-expanded={open} onClick={()=>setOpen(v=>!v)} aria-label={label}>{trigger}</button>{open?<div className="dropdown-panel" role="menu">{children}</div>:null}</div>}

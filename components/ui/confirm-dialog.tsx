'use client';import { Button } from './button';import { Modal } from './modal';export function ConfirmDialog({open,onClose,onConfirm,title,description,danger=false}:{open:boolean;onClose:()=>void;onConfirm:()=>void;title:string;description:string;danger?:boolean}){return <Modal open={open} onClose={onClose} title={title}><p className="muted">{description}</p><div className="toolbar"><Button variant="outline" onClick={onClose}>Cancel</Button><Button variant={danger?'danger':'primary'} onClick={onConfirm}>Confirm</Button></div></Modal>}
